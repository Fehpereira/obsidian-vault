if (isNumero(num.value) && !inLista(num.value) // se o valor atribuido a isNumero for um número e não conter o mesmo valor em inLista

para verificar se um número já foi adicionado:

if(l.indexOf(Number(n)) != -1)